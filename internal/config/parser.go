package config

// Config defines the configuration uses in various modules of the application
// and can also be used to define Resources and their fields

// Parser is responsible for parsing the configuration file and handle them accordingly

type Config struct {
}
